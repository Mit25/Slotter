<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.slotter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".DashboardActivity"
            android:label="Slotter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".addEvent" />
        <activity android:name=".CardViewDetails" />
        <activity android:name=".SlotSelction" />
        <activity android:name=".CreatedAuthSlot" />
        <activity android:name=".UpcomingEnroll" />
        <activity android:name=".SlotAuthorise" />
        <activity android:name=".EnrollinSlot"></activity>
    </application>

</manifest>